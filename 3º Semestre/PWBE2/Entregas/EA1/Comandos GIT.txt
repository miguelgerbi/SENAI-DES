git config --global user.name "miguelgerbi"
git config --global user.email "miguelaraujogerbi@gmail.com"

//clonar repositório
git clone https://github.com/miguelgerbi/SENAI-DES.git

//Atualizar repositório WEB
git add .  (adiciona as alterações feitas)
git commit -m "comentario"  (valida as alterações feitas)
git push  (envia as alterações)

//Atualizar repositório local
git pull